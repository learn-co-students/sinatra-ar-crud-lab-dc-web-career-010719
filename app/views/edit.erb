<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title> Edit <%= @article.title %> </title>
  </head>
  <body>
    <form action="/articles/<%= @article.id %>" method="post">
      <input type="hidden" name="_method" value="patch">
      <label for="title">Article Title: </label><input type="text" name="title" value="<%= @article.title %>" />
      <label for="name">Article Content</label> <input type="text" name="content" value="<%= @article.content %>" />
      <input type="submit" value="Submit">
    </form>
  </body>
</html>
